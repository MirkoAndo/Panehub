import Link from 'next/link'
import { ArrowLeft, CalendarDays, CheckCircle2, Clock3, Package, Phone, UserRound } from 'lucide-react'
import { createClient } from '@/lib/supabase/server'

export default async function DashboardPage() {
  const supabase = await createClient()
  const { data: reservations } = await supabase.from('reservations').select('id,reservation_code,customer_name,customer_phone,pickup_date,pickup_time_slot,status,total,created_at').order('created_at', { ascending: false }).limit(20)
  const rows = reservations ?? []
  const pending = rows.filter((row) => row.status === 'PENDING').length
  return <main className="min-h-screen bg-[#f7f1e8] text-[#30251f]"><header className="border-b border-[#dfd2c2] bg-[#fffaf3]"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5 lg:px-10"><Link href="/" className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-full bg-[#ad4e32] text-[#fffaf3]"><Package size={17} /></span><span className="font-serif text-2xl">Panevero</span></Link><Link href="/" className="flex items-center gap-2 text-sm font-semibold text-[#76675c] hover:text-[#ad4e32]"><ArrowLeft size={16} /> Vedi sito</Link></div></header><section className="mx-auto max-w-7xl px-6 py-10 lg:px-10"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-xs font-bold uppercase tracking-[.22em] text-[#ad4e32]">Area operatori</p><h1 className="mt-2 font-serif text-5xl">Buongiorno, forno.</h1><p className="mt-2 text-sm text-[#76675c]">Tieni sotto controllo le prenotazioni del tuo banco.</p></div><button className="rounded-full border border-[#d9cdbd] px-4 py-2 text-sm font-semibold text-[#76675c]">Impostazioni</button></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-[#e5d9ca] bg-[#fffaf3] p-5"><p className="text-sm text-[#76675c]">Da confermare</p><p className="mt-2 font-serif text-4xl text-[#ad4e32]">{pending}</p><p className="mt-2 flex items-center gap-1 text-xs text-[#76675c]"><Clock3 size={13} /> oggi</p></div><div className="rounded-2xl border border-[#e5d9ca] bg-[#fffaf3] p-5"><p className="text-sm text-[#76675c]">Prenotazioni totali</p><p className="mt-2 font-serif text-4xl">{rows.length}</p><p className="mt-2 flex items-center gap-1 text-xs text-[#76675c]"><CalendarDays size={13} /> ultimi ordini</p></div><div className="rounded-2xl border border-[#e5d9ca] bg-[#fffaf3] p-5"><p className="text-sm text-[#76675c]">Stato banco</p><p className="mt-2 flex items-center gap-2 font-serif text-3xl"><span className="size-3 rounded-full bg-[#6b8d5b]" /> Operativo</p><p className="mt-2 text-xs text-[#76675c]">Ritiro attivo</p></div></div><div className="mt-10 overflow-hidden rounded-2xl border border-[#e5d9ca] bg-[#fffaf3]"><div className="flex items-center justify-between border-b border-[#e5d9ca] px-5 py-4"><h2 className="font-serif text-2xl">Ultime prenotazioni</h2><span className="rounded-full bg-[#f1e8dc] px-3 py-1 text-xs font-semibold text-[#76675c]">{rows.length} ordini</span></div><div className="divide-y divide-[#e5d9ca]">{rows.length === 0 ? <div className="p-8 text-center text-sm text-[#76675c]">Ancora nessuna prenotazione. Gli ordini appariranno qui.</div> : rows.map((row) => <div key={row.id} className="flex flex-col gap-4 px-5 py-5 md:flex-row md:items-center md:justify-between"><div><div className="flex items-center gap-3"><span className="font-mono text-xs font-semibold text-[#ad4e32]">{row.reservation_code}</span><span className={`rounded-full px-2.5 py-1 text-[11px] font-bold ${row.status === 'PENDING' ? 'bg-[#f6dfb7] text-[#80551e]' : 'bg-[#dce7d7] text-[#496044]'}`}>{row.status}</span></div><p className="mt-2 flex items-center gap-2 font-semibold"><UserRound size={15} className="text-[#ad4e32]" /> {row.customer_name}</p><p className="mt-1 flex items-center gap-2 text-xs text-[#76675c]"><Phone size={13} /> {row.customer_phone}</p></div><div className="flex items-center gap-8 text-sm"><div><p className="text-xs text-[#76675c]">Ritiro</p><p className="mt-1 font-semibold">{row.pickup_date} · {row.pickup_time_slot}</p></div><div className="text-right"><p className="text-xs text-[#76675c]">Totale</p><p className="mt-1 font-semibold text-[#ad4e32]">€ {Number(row.total).toFixed(2)}</p></div><button className="grid size-9 place-items-center rounded-full border border-[#d9cdbd] text-[#ad4e32]" aria-label="Segna come pronto"><CheckCircle2 size={17} /></button></div></div>)}</div></div></section></main>
}
